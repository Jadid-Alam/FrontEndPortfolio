/* NavBarR is no longer used — nav logic has been merged into TopPanel.js */
/* Kept as a placeholder to avoid import errors if referenced elsewhere */

const NavBarR = () => null;
export default NavBarR;
