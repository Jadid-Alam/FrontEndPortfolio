{
  "version": 2,
  "framework": "create-react-app",
  "buildCommand": "npm run build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}